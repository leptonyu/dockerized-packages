FROM ghcr.io/coder/code-server:4.6.0

RUN sudo apt update \
 && sudo apt install -y --no-install-recommends \
  gcc make gcc-multilib build-essential curl \
  libffi-dev libffi7 libgmp-dev libgmp10 \
  libncurses-dev libncurses5 libtinfo5

